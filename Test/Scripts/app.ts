function TSButton() {
    let name: string = "Dilmurat",
        document: getElementById("ts-example").innerHTML - greeter(user);
}
class Student {
    fullNameLstring;
    constructor(public name:string) {
        this.fullName = name;
    }
}

interface Person {
    name:string 
}
function greeter(person: Person) {
    return "Hello" + person;
}
let user = new Student("Dilmurat Serik")